<Window x:Class="BeiShuiCS2.JoinRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:e="clr-namespace:BeiShuiCS2"
        Title="房间大厅" 
        Width="1100" Height="800"
        WindowStartupLocation="CenterOwner"
        MinWidth="900" MinHeight="600" Style="{DynamicResource AuroraWindow}" WindowStyle="None" AllowsTransparency="True">

    <Window.Resources>
        <!-- 房间卡片样式 -->
        <Style x:Key="RoomCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardGradientBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{StaticResource RadiusMedium}"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Width" Value="320"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Margin" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.2" Color="#000000"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1.03" Duration="0:0:0.25"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1.03" Duration="0:0:0.25"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color"
                                                To="{StaticResource PrimaryColor}"
                                                Duration="0:0:0.25"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1" Duration="0:0:0.25"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1" Duration="0:0:0.25"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color"
                                                To="#2D4A35" Duration="0:0:0.25"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Height="64" Margin="32,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE8BD;" FontFamily="Segoe Fluent Icons"
                           FontSize="28" Foreground="{StaticResource PrimaryBrush}"
                           Margin="0,0,16,0"/>
                <TextBlock Text="房间大厅" FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <Border Background="{StaticResource BgElevatedBrush}"
                        CornerRadius="{StaticResource RadiusFull}"
                        Padding="8,4" Margin="16,0,0,0" Width="72">
                    <TextBlock x:Name="txtRoomCount" Text="6 个房间"
                               FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Border>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="创建房间" Style="{StaticResource BtnPrimary}"
                        Width="120" Height="45" Click="CreateRoom_Click">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="20" ShadowDepth="0"
                                          Opacity="0.4" Color="{StaticResource PrimaryColor}"/>
                    </Button.Effect>
                </Button>
                <Button Content="&#xE8BB;" FontFamily="Segoe Fluent Icons"
                        Style="{StaticResource WindowControlBtn}"
                        Margin="16,0,0,0" Click="Close_Click"/>
            </StackPanel>
        </Grid>

        <!-- 搜索和筛选 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="32,0,32,16">
            <Border Background="{StaticResource BgElevatedBrush}"
                    CornerRadius="{StaticResource RadiusMedium}"
                    BorderBrush="{StaticResource BorderPrimaryBrush}"
                    BorderThickness="1" Width="300" Height="44">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe Fluent Icons"
                               FontSize="16" Foreground="{StaticResource TextMutedBrush}"
                               Margin="16,0,0,0" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="txtSearch"
                             Background="Transparent" BorderThickness="0"
                             Padding="12,0" Foreground="{StaticResource TextPrimaryBrush}"
                             FontSize="14" CaretBrush="{StaticResource PrimaryBrush}"
                             TextChanged="TxtSearch_TextChanged"/>
                </Grid>
            </Border>
            <Button Content="全部" Style="{StaticResource BtnPrimary}"
                    Width="80" Height="44" Margin="12,0,0,0" Click="FilterAll_Click"/>
            <Button Content="竞技" Style="{StaticResource BtnGhost}"
                    Width="80" Height="44" Margin="8,0,0,0" Click="FilterCompetitive_Click"/>
            <Button Content="休闲" Style="{StaticResource BtnGhost}"
                    Width="80" Height="44" Margin="8,0,0,0" Click="FilterCasual_Click"/>
        </StackPanel>

        <!-- 房间列表 -->
        <ScrollViewer Grid.Row="2" Margin="20,0,20,20"
                      VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="roomContainer" HorizontalAlignment="Center">
                <!-- 动态生成房间卡片 -->
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</Window>

