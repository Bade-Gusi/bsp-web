<Window x:Class="BeiShuiCS2.LoginHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登录历史"
        Width="700" Height="569"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgDeepBrush}" Style="{DynamicResource AuroraWindow}" WindowStyle="None" AllowsTransparency="True">

    <Grid Margin="28">
        <Grid Height="51" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="登录历史" FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Top" Margin="0,2,0,0"/>
            <TextBlock Grid.Column="0" Text="查看最近登录设备和位置" FontSize="12"
                       Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Top" Margin="0,35,0,0"/>
            <Button Grid.Column="1" Content="&#xE8BB;" FontFamily="Segoe Fluent Icons"
                    Style="{StaticResource WindowControlBtn}"
                    Click="Close_Click" VerticalAlignment="Center"/>
        </Grid>

        <Border Margin="0,56,0,95"
                Background="{StaticResource BgCardBrush}"
                BorderBrush="{StaticResource BorderPrimaryBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource RadiusMedium}">
            <DataGrid x:Name="dgLoginHistory"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Background="Transparent"
                      Foreground="{StaticResource TextPrimaryBrush}"
                      BorderThickness="0"
                      RowBackground="Transparent"
                      AlternatingRowBackground="{StaticResource BgElevatedBrush}"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      SelectionMode="Single"
                      FontSize="13"
                      RowHeight="40"
                      Padding="16,0" Height="361" VerticalAlignment="Top">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderPrimaryBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="16,8"/>
                        <Setter Property="Height" Value="36"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="16,0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="160"/>
                    <DataGridTextColumn Header="设备" Binding="{Binding Device}" Width="150"/>
                    <DataGridTextColumn Header="IP 地址" Binding="{Binding IP}" Width="130"/>
                    <DataGridTextColumn Header="位置" Binding="{Binding Location}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border VerticalAlignment="Top" Background="{StaticResource BgElevatedBrush}"
                CornerRadius="{StaticResource RadiusMedium}" Padding="16,12" Margin="0,436,0,0" Height="70">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="如有异常登录记录，请立即修改密码"
                           Foreground="{StaticResource TextMutedBrush}" FontSize="12" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="知道了" Style="{StaticResource BtnPrimary}"
                        Width="100" FontSize="12" Click="Close_Click" Margin="0,0,0,6"/>
            </Grid>
        </Border>
    </Grid>
</Window>
