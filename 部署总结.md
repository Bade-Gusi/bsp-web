# 背水对战平台 - 服务器部署总结

## 服务器信息

- **IP**: hni1.wch1.top
- **SSH 端口**: 35153
- **系统**: Ubuntu 22.04 LTS
- **用户**: root
- **密码**: Hx5532999

## 已安装组件

| 组件 | 版本 | 状态 | 说明 |
|------|------|------|------|
| MySQL | 8.0.45 | ✅ | root / Beishui@2026 / 库 beishui |
| PostgreSQL | 14 | ✅ | postgres / Beishui@2026 / 库 beishui |
| .NET | 待安装 | ⏳ | 需要运行安装命令 |
| Docker | 已装 | ✅ | docker.io + docker-compose-v2 |
| Coturn (TURN) | 已装 | ✅ | 用于语音/屏幕分享穿透 |
| UFW 防火墙 | 已启用 | ✅ | 已开放端口: 22, 80, 443, 5000, 3478/udp, 49152-65535/udp |

## 项目目录

- `/opt/beishui/api` — 后端 API
- `/opt/beishui/database` — 数据库备份
- `/opt/beishui/logs` — 日志
- `/opt/beishui/turn` — TURN 配置

## 待完成

### 1. 安装 .NET 9.0（如果有需要的话）

```bash
wget -q https://dot.net/v1/dotnet-install.sh -O /tmp/dotnet-install.sh
chmod +x /tmp/dotnet-install.sh
/tmp/dotnet-install.sh --channel 9.0 --install-dir /usr/share/dotnet
ln -sf /usr/share/dotnet/dotnet /usr/local/bin/dotnet
dotnet --version
```

### 2. 部署后端 API

将编译好的后端程序上传到 `/opt/beishui/api`，然后运行：

```bash
dotnet /opt/beishui/api/BeishuiServer.dll --urls "http://0.0.0.0:5000"
```

### 3. 配置 Coturn

编辑 `/etc/turnserver.conf`：

```
listening-port=3478
fingerprint
lt-cred-mech
user=beishui:Beishui@2026
realm=hni1.wch1.top
```

启动：`systemctl enable --now coturn`

## 数据库连接

```
MySQL:    localhost:3306  root / Beishui@2026 / beishui
PostgreSQL: localhost:5432  postgres / Beishui@2026 / beishui
```

## 本地开发环境

- **项目路径**: `D:\学习\ddddddddddfxxg\bsp\`
- **框架**: .NET 9.0 WPF
- **NuGet 包**: NAudio (语音), LiteNetLib (UDP), WebView2
