<Window x:Class="BeiShuiCS2.SkinMarketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="皮肤市场" 
        Width="1000" Height="800"
        WindowStartupLocation="CenterOwner"
        MinWidth="800" MinHeight="600" Style="{DynamicResource AuroraWindow}" WindowStyle="None" AllowsTransparency="True">

    <Window.Resources>
        <!-- 皮肤卡片 -->
        <Style x:Key="SkinCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardGradientBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{StaticResource RadiusMedium}"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="260"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Effect" Value="{StaticResource ShadowMedium}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1.05" Duration="0:0:0.2"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1.05" Duration="0:0:0.2"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color"
                                                To="{StaticResource PrimaryColor}"
                                                Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1" Duration="0:0:0.2"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1" Duration="0:0:0.2"
                                                 EasingFunction="{StaticResource EaseOutCubic}"/>
                                <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color"
                                                To="#2D4A35" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Height="64" Margin="32,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE7C3;" FontFamily="Segoe Fluent Icons"
                           FontSize="28" Foreground="{StaticResource AccentBrush}"
                           Margin="0,0,16,0"/>
                <StackPanel>
                    <TextBlock Text="皮肤市场" Style="{StaticResource PageHeader}"/>
                    <TextBlock x:Name="txtBalance" Text="余额: 1,250 点"
                               FontSize="12" Foreground="{StaticResource PrimaryBrush}"/>
                </StackPanel>
            </StackPanel>
            <Button Grid.Column="1" Content="&#xE8BB;" FontFamily="Segoe Fluent Icons"
                    Style="{StaticResource WindowControlBtn}"
                    Click="Close_Click"/>
        </Grid>

        <!-- 搜索和筛选 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="32,0,32,16">
            <Border Background="{StaticResource BgElevatedBrush}"
                    CornerRadius="{StaticResource RadiusMedium}"
                    BorderBrush="{StaticResource BorderPrimaryBrush}"
                    BorderThickness="1" Width="250" Height="44">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe Fluent Icons"
                               FontSize="16" Foreground="{StaticResource TextMutedBrush}"
                               Margin="12,0,0,0" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="txtSearch"
                             Background="Transparent" BorderThickness="0"
                             Padding="12,0" Foreground="{StaticResource TextPrimaryBrush}"
                             FontSize="14" CaretBrush="{StaticResource PrimaryBrush}"/>
                </Grid>
            </Border>
            <Button Content="全部" Style="{StaticResource BtnPrimary}"
                    Width="80" Height="44" Margin="12,0,0,0" Click="FilterAll_Click"/>
            <Button Content="步枪" Style="{StaticResource BtnGhost}"
                    Width="80" Height="44" Margin="8,0,0,0" Click="FilterRifle_Click"/>
            <Button Content="手枪" Style="{StaticResource BtnGhost}"
                    Width="80" Height="44" Margin="8,0,0,0" Click="FilterPistol_Click"/>
            <Button Content="刀具" Style="{StaticResource BtnGhost}"
                    Width="80" Height="44" Margin="8,0,0,0" Click="FilterKnife_Click"/>
        </StackPanel>

        <!-- 稀有度筛选 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="32,0,32,16">
            <TextBlock Text="稀有度:" FontSize="12" Foreground="{StaticResource TextMutedBrush}"
                       VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="普通" Style="{StaticResource BtnGhost}"
                    Width="69" Height="40" FontSize="11" Margin="0,0,8,0"/>
            <Button Content="罕见" Style="{StaticResource BtnGhost}"
                    Width="72" Height="40" FontSize="11" Margin="0,0,8,0"/>
            <Button Content="史诗" Style="{StaticResource BtnGhost}"
                    Width="70" Height="40" FontSize="11" Margin="0,0,8,0"/>
            <Button Content="传说" Style="{StaticResource BtnGhost}"
                    Width="75" Height="39" FontSize="11" Margin="0,0,8,0"/>
            <Button Content="神话" Style="{StaticResource BtnGhost}"
                    Width="73" Height="40" FontSize="11"/>
        </StackPanel>

        <!-- 皮肤网格 -->
        <ScrollViewer Grid.Row="3" Margin="22,0,22,22"
                      VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="skinContainer" HorizontalAlignment="Center">
                <!-- 动态生成 -->
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</Window>
