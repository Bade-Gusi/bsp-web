<Window x:Class="BeiShuiCS2.ConfirmDeleteFriendWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认删除" 
        Width="420" Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Style="{DynamicResource AuroraWindow}" WindowStyle="None" AllowsTransparency="True">


    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="&#xE7BA;" FontFamily="Segoe Fluent Icons"
                   FontSize="40" Foreground="{StaticResource DangerBrush}"
                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="1" Text="确认删除好友?" FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   HorizontalAlignment="Center" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="2" x:Name="txtFriendName" Text="确定要删除该好友吗?"
                   FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"
                   HorizontalAlignment="Center" TextWrapping="Wrap"
                   TextAlignment="Center" Margin="0,0,0,24"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button Content="取消" Style="{StaticResource BtnGhost}"
                    Width="120" Height="48" Margin="0,0,12,0"
                    Click="Cancel_Click"/>
            <Button Content="删除" Style="{StaticResource BtnDanger}"
                    Width="120" Height="48" Click="Delete_Click"/>
        </StackPanel>
    </Grid>
</Window>
