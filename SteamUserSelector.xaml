<Window x:Class="BeiShuiCS2.SteamUserSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择 Steam 用户" 
        Width="420" Height="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Style="{DynamicResource AuroraWindow}" WindowStyle="None" AllowsTransparency="True">

        <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="&#xE774;" FontFamily="Segoe Fluent Icons"
                   FontSize="36" Foreground="{StaticResource PrimaryBrush}"
                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="1" Text="选择 Steam 用户" FontSize="20" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   HorizontalAlignment="Center" Margin="0,0,0,24"/>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="usersPanel" Margin="12">
                    <!-- 动态生成 -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Button Grid.Row="3" Content="取消" Style="{StaticResource BtnGhost}"
                Width="120" Height="44" Margin="0,16,0,0"
                HorizontalAlignment="Center" Click="Cancel_Click"/>
    </Grid>
</Window>
